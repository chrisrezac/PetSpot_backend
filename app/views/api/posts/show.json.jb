post = {
  title: @post.title,
  body: @post.body,
  image_url: @post.image_url,
  post_id: @post.id,
  pet_id: @post.pet_id
}
post[:pet] = {
  pet_name: @post.pet.name,
}
post[:comments] = {
  comment: @post.comments
}
post